import { describe, it, expect } from "vitest";

describe("format utilities", () => {
  it("should verify test setup works", () => {
    expect(1 + 1).toBe(2);
    expect("hiretrack").toContain("hire");
  });
});
